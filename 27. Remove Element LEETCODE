class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        arr = []
        k = 0
        for i in nums:
            if i!=val:
                arr.append(i)
                k+=1
        for j in range(0,len(arr)):
            nums[j] = arr[j]
        return k
